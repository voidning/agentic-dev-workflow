---
name: agentic-dev-workflow
description: 通用 AI 工程协作流程，用于开发或修改代码、Skill、可复用提示词、Agent 配置和自动化流程，按任务风险组织需求、基线、验收标准、实现、自检、按需独立验收与交付。适用于用户要求这套协作方式，或需要制作并验证上述工程成果的任务；纯知识问答、普通文案润色不触发，纯审查或规划只执行相应只读阶段。
---

# AI 工程协作

用可检查的结果完成代码、Skill、可复用提示词、Agent 配置和自动化等工程任务。主 Agent 对范围、推进与最终陈述负责；实现者自检；按风险与复杂度决定是否独立验收。用户负责目标和关键取舍，无需逐步指挥常规实现。

本 Skill 组织协作，不替代成果类型对应的制作方法。创建或修改 Skill 时结合可用的 skill-creator；其他任务按实际需要使用专门能力，不强制引入工具或框架。

## 工作边界与角色

- 遵循用户当前请求、既有授权、项目规则和可用工具。本 Skill 不扩大修改、发布、购买或外部通信的权限，不修改权限配置以绕过限制。
- 小任务默认由主 Agent 实现并针对性自检，不启动子 Agent。复杂任务可将实现分给子 Agent，主 Agent 协调。仅对能独立推进的工作并行，明确文件或模块归属，避免竞争写入。
- 高风险、跨模块复杂改动、反复失败或用户明确要求独立验收时，安排与实现分离的验收；不因成果是代码、Skill 或配置就自动委派。验收者不参与被验收实现，不写入被验收成果、验收标准或已有测试；其临时证据放在隔离位置。每个独立验收任务使用新上下文；修复后的同一验收者可以继续复验，但不能只检查实现者给出的结论。
- 使用当前环境实际支持的子 Agent 委派工具；不要把创建用户可见的新任务当作内部委派。工具不可用、用户禁止委派或更高优先级规则禁止时，不规避限制：完成已授权且不依赖委派的工作，说明“独立验收未执行”，仅在独立验收属于本次必要条件时，将整体状态标为待验收或受阻；轻量任务可凭相关自检交付。不能把主 Agent 换口吻的自评称为独立验收。
- 使用已有模型配置；不因角色标签声称切换了模型。不同模型交叉评审可选，不构成正确性的保证。
- 主 Agent 核实结论与范围，不能仅凭子 Agent 的分数或赞同宣布完成。

## 根据任务调整深度

| 情况 | 工作方式 |
| --- | --- |
| 普通文案、布局、局部且可逆的代码或指令调整 | 主 Agent 直接实现并针对性自检；不默认委派、新增流程文档或镜像实现的测试 |
| 普通多文件功能或修复 | 简短方案，按需分批，检查关键边界与受影响路径；文件数量本身不触发独立验收 |
| 跨模块复杂改动、高风险、反复失败或用户明确要求独立验收 | 自检稳定后集中独立验收；按需记录计划与基线、隔离状态、验证兼容与恢复 |
| 探索、审查、规划 | 只完成请求的阶段；先用小实验降低关键不确定性，不自动进入正式实现 |

风险由影响范围、可逆性、状态或数据变化及不确定性决定，不按行数判断；跨模块涉及接口、依赖或状态联动时按复杂改动处理。纯知识问答和普通文案润色默认不触发；显式调用时也只做相应轻量工作。以下阶段按需采用，不是每次必须逐项执行的清单。审美、手感与产品价值保留用户判断。

## 读取与委派成本

- 先定位文件、符号或失败位置，再读相关片段；只在需要整体结构时读全文。已读且未变化的内容不重复读取，除非有明确核对需要。
- 浏览器优先获取相关状态或局部截图，日志保留关键错误和证据位置；避免反复输出完整状态与大段日志。
- 所有子 Agent 默认使用新上下文和短任务包：目标、必要背景、允许范围、验证方式、停止条件。只有确实依赖完整历史时才继承完整上下文；独立验收仍用新上下文。
- 参考文件按当前阶段读取相关部分，已读未变不重读；不为普通自检加载整套委派协议。

## 1. 接收目标并调查现状

将请求整理为目标、范围、保留行为、限制和未知项。先识别成果类型，查相关实现或指令、文档、加载/运行入口及项目规则，能从环境获得的信息不交给用户重复提供。问题集中提出，等待时继续独立工作。

确认实际工作目录、版本或文件状态，保留用户数据与无关改动。对 Bug 或指令执行偏差尽可能复现，保存症状、条件和失败证据；对现有成果建立适量基线。对于 Skill、提示词和配置，记录原内容、触发/加载方式、模型与工具环境（实际可知时）。不能复现时区分事实与假设，选择能区分原因的检查，不直接把猜测变成修复。

## 2. 确定方案与验收契约

修改前明确以下内容，小任务可记录在当前上下文，复杂任务使用项目现有任务记录：

- 目标行为：输入、操作或前提，以及可观察结果。
- 保留行为：兼容性、状态及不应变化的能力。
- 验证办法：相关命令、实际操作、对照或测量，以及通过条件。
- 范围与阻断条件：允许修改区域，必须解决的问题，尚需用户决定的取舍。

给出足以执行的最小方案。复杂、模糊或高风险任务可先委派独立方案审查，检查漏项、可测性和影响范围；这不替代实现后的独立验收。用户确认关键歧义，以及影响产品行为、成本、技术路线或主要感官体验的取舍；已有授权内的常规步骤继续推进。

验收契约可因真实需求或环境变化调整，必须说明依据与影响；不得为了让现有实现通过而降低标准。诊断发现旧测试错误时，单独说明证据，区分测试修正与产品修复。

修改本协作 Skill 或其验收协议自身时，改动前固定本轮需求与验收标准，并保留可对照的原版本；轻量任务可用当前上下文和已有版本记录，不强制新建文档。候选版本作为被测对象，不能自行重定义本轮的通过条件、委派权限或停止规则；保持一次有边界的评测，不递归启动无限层验收 Agent。

### 审美方向未明确时，先支持选择

- 涉及配音、音乐、视觉风格、动效或剪辑节奏等主要感官体验，且现有上下文不足以确定方向时，先提供可感知的参考供用户选择，再进行依赖该方向的完整制作。“加配音”等功能授权不等于认可某个声音或风格；不能仅因某个工具或素材现成可用就替用户定下方向。
- 用最快、成本最低且足以区分方向的方式提供参考，通常给出 2–3 个有实际差异的选项、简短差异说明与推荐。优先使用现成试听、图片、视频链接及具体时间点，或已有素材；确认参考可访问且确实对应所述选项。不要只给抽象风格标签，也不要为了让用户选择先制作多份完整 demo。现成参考不足时，才制作最短必要样例，并说明参考与最终可实现效果的差异。
- 等待用户选择期间，继续素材整理、格式检查等不依赖方向的工作，暂不批量生成、完整渲染或合成。等待超时不视为选定；选定后在已认可方向内自主执行，无需逐项确认。
- 用户已有明确参考、已认可的风格或明确授权自主决定时，沿用这些依据推进；局部微调且不改变主要感官体验时直接处理。不要把每次小改动都变成审美确认环节。
- 验收区分技术可用与审美适配：有音轨、能播放、尺寸正确等检查，不能证明声音、风格或节奏符合用户偏好；交付应对照已选参考说明实际效果，未获用户评价不宣称审美已通过。

## 3. 分批实现并自检

每批完成一个有意义的变化，运行与其相关的检查。优先复用已有启动与测试入口；重复操作确有收益时再封装。只增加用于验证真实行为或已确认缺陷的必要测试。

对 Skill、提示词、Agent 配置与自动化，按 [独立验收协议中的成果验证方法](references/verification.md#指令、配置与自动化的验证) 选择文件验证、规则审查和行为验证。语法或格式通过只能支持对应层级的结论，不能证明 Agent 会按要求执行。先在隔离环境验证候选成果，不因测试而替换无关配置、启用真实定时任务或扩大外部操作授权。

修 Bug 时，在可行范围先确认回归样例因目标缺陷失败，再用同一条件验证修复；检查失败原因，避免把环境错误当成缺陷复现。改动过程中不得通过删除断言、跳过失败检查或放宽阈值来掩盖问题。

静态检查、逻辑/回归测试、构造场景、正常入口验证和性能测量分别记录。构造成功状态不能证明正常路径能够到达该状态。开发测试钩子只用于已明确的检查，不代替真实输入流程，也不无意进入生产接口。

相关检查通过后，除非有新改动、新失败或未解决风险，不重复扩大测试。小修改不强制整套测试或长时间压力运行。

## 4. 独立验收

仅在需要独立验收时，于实现基本稳定并完成自检后，读取 [独立验收协议](references/verification.md) 的相关部分，按中立短任务包集中委派一次验收；不为每个小批次重复委派。让验收者拥有原始需求、约束、验收契约、目标版本、基线与运行方式，独立读取实现并重跑关键检查。

验收期间冻结被验收范围，或在明确快照上验收。不要把实现者的诊断、拟议修复或“已通过”结论当成验收提示。实现记录可作为待核实材料，不能替代独立证据。

验收者输出通过、退回或受阻，并说明覆盖与限制。每个缺陷应有需求依据、预期与实际差异、复现方法、影响及证据；推测项明确标注，不把风格偏好或额外功能建议升级为阻断缺陷。

## 5. 修复、分歧与停止

- 主 Agent 核实问题是否有证据、是否属于本次范围。实现者可提供反证，不要求其无条件照改单个评审意见。
- 确认缺陷后由实现者修复；若本次需要独立验收，由验收者在新版本复验对应问题及受影响回归，否则针对性自检。没有新改动、新失败或未解决风险，不重复全量检查。修改使相关旧验收失效；最终结论必须对应最终成果及加载/运行配置。
- 检查失败后先区分实现、模型决策、状态/历史、环境或验证工具问题，再决定修改对象；保留一次失败的关键证据。一次修复无效后形成有边界的下一步与停止条件，不无证据重复同一方案。已有自主执行授权时，范围内常规修复继续推进，不逐次求批；核心重构、破坏性操作或明显扩大范围时，先提交具体方案并确认授权。
- 缺少新证据、真实受阻或需要扩大授权时暂停依赖工作，继续不受影响的部分。不得无限修复、反复换评审者寻求通过或弱化验收。
- 现有失败必须披露；影响本次目标验证时标为受阻。无关且已确认的基线失败可单列，不伪称全项目通过。

## 6. 交付与接续

范围完成、相关检查通过、剩余限制说明清楚后即交付，不为完善而增加系统、文档、测试或审查轮次。用户明确要求的验证不能因节约成本省略。

进度与交付优先简短说明结果、实际证据和阻断问题；最终注明自检或独立验收、未验证或遗留限制，按需补充使用与回退说明。明确测试类型与实际覆盖，不把“未发现问题”升级成完整验证。

任务完成不自动授权提交、合并、发布或外部写入；按已有授权执行必要交付，不对已获授权的动作重复确认。

只维护必要的项目规则、任务状态和执行证据，优先使用已有文件。实际成果与当前记录是接续依据，不依赖长聊天的完整记忆。持续规则保持短小；重复且可判定的错误优先沉淀为测试、类型/依赖检查或标准实现。改通用规则前确认它不是本次局部偏好。

## 评估这套流程

观察完成总耗时、用户介入次数、交付后回归、重复错误与验收误报，而不是代码量或 Agent 数量。维护时先按风险选择少量代表性任务做规则推演；高风险、反复失败或明确要求实际行为验证时，再做隔离行为评测。历史失误先核对原因，不机械追加通用规则。规则推演不等于实际执行，也不能据此承诺节省比例。

Skill 文件和提示词本身不提供强制执行保证。需要机械约束时，在用户授权范围内使用项目工具链、隔离环境或自动检查；格式验证通过不等于行为可靠。
